{
  "name": "celebrity-aesthetic-suite",
  "version": "1.0.0",
  "private": true,
  "description": "Celebrity Aesthetic Suite - Clinic Management System (CRM) & Patient Website",
  "workspaces": [
    "celebrity-aesthetic",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n \"CRM,WEB\" -c \"blue,magenta\" \"npm --prefix celebrity-aesthetic run dev\" \"npm --prefix web run dev\"",
    "dev:crm": "npm --prefix celebrity-aesthetic run dev",
    "dev:web": "npm --prefix web run dev",
    "build": "npm --prefix celebrity-aesthetic run build && npm --prefix web run build",
    "build:crm": "npm --prefix celebrity-aesthetic run build",
    "build:web": "npm --prefix web run build",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
